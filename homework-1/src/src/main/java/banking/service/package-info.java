/**
 * Service layer — TransactionService, BalanceService, SummaryService.
 * Orchestrates validation, storage access, and domain calculation via the
 * FilterEngine, BalanceCalculator, and SummaryCalculator components.
 */
package banking.service;
