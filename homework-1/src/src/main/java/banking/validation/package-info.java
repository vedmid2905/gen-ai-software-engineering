/**
 * Validation layer — Validator component.
 * Collects all field-level errors before throwing ValidationException
 * so callers always receive a complete error list in a single 400 response.
 */
package banking.validation;
