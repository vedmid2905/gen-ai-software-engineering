spring.application.name=banking-transactions-api
server.port=8080

# banking.pipeline — leave input-file blank to auto-resolve sample-transactions.json
# (checks the working directory, then its parent)
pipeline.shared-dir=shared
pipeline.input-file=
